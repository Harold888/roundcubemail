<?php
// plugins/contextmenu/config.inc.php

// Configuración específica del ContextMenu
$config['contextmenu_actions'] = array(
    'reply'       => true,
    'reply_all'   => true,
    'forward'     => true,
    'mark_read'   => true,
    'mark_unread' => true,
    'delete'      => true,
    'move'        => true,  // Asegúrate de tener carpetas configuradas
    'copy'        => false, // Deshabilitado si no se usa
    'print'       => true,
    'download'    => true,
    'view_source' => false, // Opcional: deshabilitar vista de código fuente
);

// Carpetas donde se mostrará el menú (ajusta según tu IMAP)
$config['contextmenu_folders'] = array('INBOX', 'Sent', 'Trash');

// Habilitar en lista de mensajes y vista de mensaje
$config['contextmenu_enable_list'] = true;
$config['contextmenu_enable_message'] = true;